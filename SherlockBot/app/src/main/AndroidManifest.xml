<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sherlockbot.clubespe.espe.edu.ec.sherlockbot" >
    <uses-permission android:name="com.google.android.things.permission.MANAGE_INPUT_DRIVERS" />
    <uses-permission android:name="com.google.android.things.permission.USE_PERIPHERAL_IO" />
    <application android:name=".App" android:label="@string/app_name" android:theme="@style/Theme.AppCompat.Light">
        <uses-library android:name="com.google.android.things" />

        <activity android:name=".BotActivity" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Make this the first activity that is displayed when the device boots. -->
        </activity>
    </application>

</manifest>